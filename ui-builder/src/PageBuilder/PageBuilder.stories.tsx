import React from "react";
import { PageBuilder } from "./PageBuilder";

export default {
    title: "Page Builder",
};

export const Basic = () => {
    return (
        <div className="h-screen">
            <PageBuilder />
        </div>
    );
};
